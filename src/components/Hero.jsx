import React from "react";

const Hero = () => {
  return (
    <section className="hero">
      <h1 style={{ color: 'var(--primary'}}>CrossFit Studio</h1>
      <p>Empowering women through movement, not machines.</p>
            <a href="#trial-form" className="cta-button">Try 4 Sessions On Us</a>

    </section>
  );
};

export default Hero;